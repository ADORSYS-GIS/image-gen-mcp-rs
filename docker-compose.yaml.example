services:
  image-mcp:
    build: .
    ports:
      - "8080:8080"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - HOST=0.0.0.0
      - PORT=8080
      - NANO_BANANA=true
      - OUTPUT_FORMAT=file
      - OUTPUT_DIR=/app/outputs
      - RUST_LOG=info
    volumes:
      - ./outputs:/app/outputs
    restart: unless-stopped
